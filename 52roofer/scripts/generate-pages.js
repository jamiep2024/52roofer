const fs = require("fs");
const path = require("path");

const CONFIG = {
  NEXT_PAGES_DIR: "pages",
  FIXED_PAGES_DIR: "fixed-pages",
  currentDateTime: "2024-12-24 23:56:10",
  userLogin: "jamiep2024",
};

// Rest of your generate-pages.js code
